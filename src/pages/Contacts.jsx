import React from 'react';
import { Outlet } from 'react-router-dom';

const Contacts = () => {
    return (
        <div>
            contacts
              contactspage
        </div>
    );
}

export default Contacts;
