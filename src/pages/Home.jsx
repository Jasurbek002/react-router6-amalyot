import React from 'react';
import { Outlet } from 'react-router-dom';

const Home = () => {
    return (
        <div>
            home
        home page
        </div>
    );
}

export default Home;
